EXTRACT_PROMPT = """
You are reading a resume image. Your ONLY job is to extract every single word of text.

Rules:
- Extract ALL text exactly as it appears on the page
- Do NOT summarize, restructure, or paraphrase anything
- Do NOT skip any section — name, contact, experience, education, skills, projects, certifications
- Do NOT add JSON, markdown, or any formatting
- Return plain text only — nothing else

Begin extraction now.
"""
